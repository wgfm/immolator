# Immolator

Immolator is a GameBoy Colour emulator, implemented in Rust.

It is solely educational, and in the current state utterly unusable. It is also bug-ridden, until I figure out debugging.

## Building

This is a very standard Rust project, so build with Cargo:

```
cargo build
```
